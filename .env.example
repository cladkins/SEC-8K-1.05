# SEC fair-access policy requires real contact info in the User-Agent header.
# Format: "Your Name your-email@domain"
SEC_USER_AGENT="Chris Adkins cladkins@gmail.com"

# Email (shared)
SENDER_EMAIL=
RECIPIENT_EMAIL=

# Email via SendGrid (--email sendgrid)
SENDGRID_API_KEY=

# Email via SMTP (--email smtp)
SMTP_SERVER=
SMTP_PORT=587
SMTP_USERNAME=
SMTP_PASSWORD=
